{
  "name": "english-monorepo",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "pnpm --filter @english/web dev",
    "build": "pnpm --filter @english/web build",
    "start": "pnpm --filter @english/web start",
    "lint": "pnpm --filter @english/web lint",
    "lint:fix": "pnpm --filter @english/web lint:fix",
    "format": "pnpm --filter @english/web format",
    "format:check": "pnpm --filter @english/web format:check",
    "type-check": "pnpm --filter @english/web type-check",
    "test": "pnpm --filter @english/web test",
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx,js,jsx}": [
      "pnpm --filter @english/web lint:fix --",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.6.2"
  }
}
